# MQTT Structure

```text
    {companyId}/
        {embeddedId}/
            temperature: double (celsius)
            sale: itemnumber, price, timestamp, method (cash, cashless)
            online: true/false
```

